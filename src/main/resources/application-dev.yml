spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:p6spy:postgresql://152.67.196.0:5432/postgres
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
server:
  port: 9090
  address: 0.0.0.0

decorator:
  datasource:
    p6spy:
      enable: false

logging:
  level:
    p6spy: INFO
  file:
    name: /logs/app.log

file:
  upload:
    path: /app/upload
    server: /upload

app:
  frontend-url: http://168.107.63.120:3000
  mobile-redirect-scheme: motive-toy://oauth-callback
