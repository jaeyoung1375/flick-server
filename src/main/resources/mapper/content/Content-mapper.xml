<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.flick.content.mapper.ContentMapper">

    <!-- 작품 목록 조회 -->
    <select id="getContentList" resultType="ContentResponseDto">
        SELECT
                   CONTENT_ID         /* 작품아이디 */
                 , TITLE              /* 제목 */
                 , SYNOPSIS           /* 줄거리 */
                 , THUMBNAIL_FILE_ID  /* 포스터이미지 파일아이디 */
                 , RELEASE_DATE       /* 개봉일 */
                 , AGE_RATING         /* 연령등급 */
                 , CONTENT_TYPE       /* 타입 (MOVIE/SERIES) */
                 , PUBLISH_STATUS     /* 공개상태 */
        FROM CONTENT
        WHERE PUBLISH_STATUS = 'PUBLISHED'
    </select>

</mapper>
