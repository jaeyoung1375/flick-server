# 기술 스택 (Tech Stack)

> 이 디렉토리의 사실 정본은 [`flick-server-tech-stack.md`](./flick-server-tech-stack.md)입니다. 버전이 바뀌면 `build.gradle`을 먼저 확인하고 이 문서를 갱신하세요.

## 갱신 규칙
* 라이브러리 버전은 `build.gradle`이 정본 — 이 문서는 사본이다. 어긋나면 `build.gradle`을 신뢰하고 이 문서를 고친다.
* 새 의존성 추가 시 이 문서에도 한 줄 추가한다.

## 참고
* 패키지 구조: [`../03_directory_structure/flick-server-directory.md`](../03_directory_structure/flick-server-directory.md)